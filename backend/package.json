{
  "name": "backend",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "rabbit:up": "docker-compose up -d rabbitmq",
    "rabbit:down": "docker-compose down",
    "start:dev": "concurrently -n user,report,claim,notif,gateway -c blue,green,yellow,magenta,cyan \"npm run start:dev --prefix user_services\" \"npm run start:dev --prefix report_services\" \"npm run start:dev --prefix claim_services\" \"npm run start:dev --prefix notification_services\" \"npm run start:dev --prefix api_gateway\""
  },
  "keywords": [],
  "type": "module",
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "cors": "^2.8.6",
    "jsonwebtoken": "^9.0.3",
    "nanoid": "^5.1.11"
  }
}
